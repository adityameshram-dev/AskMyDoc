import 'package:flutter/material.dart';

class AppColors {
  // Primary brand palette
  static const primary = Color(0xFF4F46E5);
  static const primaryDark = Color(0xFF4338CA);
  static const primarySoftLight = Color(0xFFEEF2FF);
  static const primarySoftDark = Color(0xFF1E1B4B);

  // Accent / highlight
  static const accent = Color(0xFF8B5CF6);
  static const accentSoft = Color(0xFFE9D5FF);
  static const accentSoftDark = Color(0xFF2E1065);

  // Backgrounds
  static const backgroundLight = Color(0xFFF4F7FB);
  static const backgroundDark = Color(0xFF0F172A);
  static const surfaceLight = Color(0xFFFFFFFF);
  static const surfaceDark = Color(0xFF111827);
  static const surfaceElevatedDark = Color(0xFF1F2937);
  static const sidebarLight = Color(0xFFF8FAFF);
  static const sidebarDark = Color(0xFF0F172A);

  // Text
  static const textPrimaryLight = Color(0xFF0F172A);
  static const textPrimaryDark = Color(0xFFE2E8F0);
  static const textSecondaryLight = Color(0xFF475569);
  static const textSecondaryDark = Color(0xFF94A3B8);

  // Border / divider
  static const borderLight = Color(0xFFE2E8F0);
  static const borderDark = Color(0xFF334155);

  // Status colors
  static const success = Color(0xFF10B981);
  static const successSoft = Color(0xFFDCFCE7);
  static const error = Color(0xFFEF4444);
  static const errorSoft = Color(0xFFFEE2E2);

  // Message colors
  static const userBubbleLight = Color(0xFFE8E7FF);
  static const userBubbleDark = Color(0xFF312E81);
  static const aiBubbleLight = Color(0xFFFFFFFF);
  static const aiBubbleDark = Color(0xFF1F2937);

  // Utility
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
}
